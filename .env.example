# Combined local PostgreSQL + pgvector + Apache AGE environment
DATABASE_URL=postgresql://kiduna:kiduna_dev@localhost:55432/kiduna

# Optional real email verification. Without these, development shows a labeled fallback link.
RESEND_API_KEY=
RESEND_EMAIL_DOMAIN=
