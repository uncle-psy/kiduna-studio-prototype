'use client'

// Building / Studio layout settings — renders inside StudioHeader + StudioSidebar.
// Shares the same UI as the Active settings (/account) via MemberSettings.
export { default } from '@/components/settings/MemberSettings'
